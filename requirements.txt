# Offline Triage Pre-Screen MVP
# Python 3.10+ required

# =============================================================================
# Web Framework
# =============================================================================
fastapi==0.104.1
uvicorn[standard]==0.24.0

# =============================================================================
# Data validation
# =============================================================================
pydantic==2.5.2

# =============================================================================
# Local LLM inference (Multi-Model Engine)
# =============================================================================
# CPU-only (default):
#   pip install llama-cpp-python==0.2.20
#
# GPU (CUDA) - requires CUDA toolkit first:
#   sudo apt install nvidia-cuda-toolkit -y
#   pip uninstall llama-cpp-python -y
#   CUDACXX=/usr/bin/nvcc CMAKE_ARGS="-DGGML_CUDA=on" FORCE_CMAKE=1 pip install llama-cpp-python --no-cache-dir --force-reinstall
#
# GPU (ROCm/AMD):
#   CMAKE_ARGS="-DGGML_HIPBLAS=on" pip install llama-cpp-python --no-cache-dir --force-reinstall
#
# GPU (Metal/macOS):
#   CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python --no-cache-dir --force-reinstall
llama-cpp-python>=0.3.0

# =============================================================================
# Model Download Utility
# =============================================================================
requests>=2.31.0
tqdm>=4.66.0

# =============================================================================
# Async & File Operations
# =============================================================================
aiofiles==23.2.1

# =============================================================================
# PDF Generation
# =============================================================================
reportlab>=4.0.0

# =============================================================================
# DrBERT / HuggingFace Transformers (Optional - for French medical BERT)
# =============================================================================
# Install for DrBERT support:
#   pip install transformers accelerate torch
#
# Note: torch will auto-detect GPU (CUDA/ROCm/MPS)
# DrBERT auto-splits between GPU and CPU based on available VRAM
transformers>=4.35.0
accelerate>=0.25.0
# torch is installed separately based on your hardware:
#   CPU only:  pip install torch
#   CUDA:      pip install torch --index-url https://download.pytorch.org/whl/cu121
#   ROCm:      pip install torch --index-url https://download.pytorch.org/whl/rocm5.6

# =============================================================================
# French Pivot RAG (Protocol Retrieval-Augmented Generation)
# =============================================================================
# ChromaDB - Local vector database for semantic search
chromadb>=0.4.22
# Sentence Transformers - Alternative embedding model (optional, DrBERT uses transformers)
sentence-transformers>=2.2.2

# =============================================================================
# Development tools
# =============================================================================
python-multipart==0.0.6
